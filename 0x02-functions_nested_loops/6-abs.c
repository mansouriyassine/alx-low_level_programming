#include "main.h"

/**
 * _abs - Computes the absolute value of a number
 * @n: the number to check
 *
 * Return: n if positive, -n if negative
 */
int _abs(int)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
